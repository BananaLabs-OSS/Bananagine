name = "bananagine-composition-probe"
version = "0.1.0"
wasm = "bananagine-composition-probe.wasm"

provides = []
consumes = ["orchestrator.dispatch"]
depends_on = ["bananagine-lua"]
capabilities = ["transport.http.inbound"]
