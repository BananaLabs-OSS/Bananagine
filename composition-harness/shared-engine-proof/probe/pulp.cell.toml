name = "bananagine-shared-engine-proof-probe"
version = "0.1.0"
wasm = "bananagine-shared-engine-proof-probe.wasm"

provides = []
consumes = ["orchestrator.dispatch", "template-catalog.v1.replace"]
depends_on = ["bananagine-shared-engine-proof-lua", "template-catalog"]
capabilities = ["transport.http.inbound"]
