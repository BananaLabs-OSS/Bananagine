name = "template-catalog"
version = "0.1.0"
wasm = "template-catalog.wasm"
wasm_sha256 = "f05321b6558224d86329dbaea7148689e722d2fee85deeb921352a3265a15a92"

provides = [
  "template-catalog.v1.replace",
  "template-catalog.v1.list",
  "template-catalog.v1.get",
  "template-catalog.v1.snapshot.export",
  "template-catalog.v1.snapshot.import",
]
consumes = []
depends_on = []
capabilities = []

# Same trust, lifecycle, and host-authority boundary as the registry owner.
[execution]
mode = "fusible"
group = "runtime-catalog-state"
abi = "pulp-linear-v1"
